/**
 * agent-event/1 + agent-forest/1 — re-exported from the @dtm/core mirror.
 * Canonical schema: server-config setpoint-evals/agent-event-schema/ (never re-declare here).
 */
export type {
  AgentLifecycle,
  AgentErrorReason,
  KnownProvider,
  KnownKind,
  AgentEvent,
  AgentForestNode,
  AgentForest,
} from '@dtm/core';
