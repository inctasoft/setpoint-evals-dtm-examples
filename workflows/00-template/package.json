{
  "name": "@dtm-workflows/00-template",
  "version": "0.1.0",
  "description": "DTM workflow template — copy this directory to create a new workflow",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "echo 'Template — not compiled. Copy this directory and adapt for your workflow.'",
    "build:workers": "esbuild workers/src/handlers/*.ts --bundle --platform=node --target=node18 --outdir=workers/dist",
    "test:ste": "./ste/run-all.sh"
  },
  "dependencies": {
    "@dtm/core": "workspace:*",
    "@dtm/worker-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "esbuild": "^0.19.0"
  }
}
