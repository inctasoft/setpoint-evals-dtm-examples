{
  "name": "@dtm-workflows/plan-execution",
  "version": "1.0.0",
  "description": "Plan execution workflow — voice-assistant chunk execution via DTM orchestrator",
  "main": "dist/workflow.config.js",
  "types": "dist/workflow.config.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dtm/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
