{
  "name": "@dtm-workflows/infra-provisioning-workers",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Lambda workers for infra-provisioning workflow",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "node esbuild.config.js",
    "build:watch": "node esbuild.config.js --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dtm-workflows/infra-provisioning-typeorm": "workspace:*",
    "@dtm/worker-sdk": "workspace:*",
    "@aws-sdk/client-sqs": "^3.1098.0",
    "axios": "^1.19.0",
    "typeorm": "^0.3.27",
    "pg": "^8.22.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^20.10.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.3.0"
  }
}
