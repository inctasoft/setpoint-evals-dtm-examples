STANDING INSTRUCTIONS (injected automatically on every prompt):

1. DIFFICULTIES LOG: As you work, whenever you encounter any difficulty,
   unexpected behavior, confusing documentation, missing config, race condition,
   flaky test, DX friction, or anything that slowed you down or required a
   workaround — immediately append it to sms/DIFFICULTIES-LOG.md.
   Format: severity, status, description, root cause if known, fix/workaround.
   Do NOT wait until the end. Log difficulties as soon as you encounter them.

2. If you fix the difficulty on the fly, mark it as FIXED with the solution.
   If you cannot fix it, mark it as OPEN for later review.

3. At the end of your response, briefly summarize any new difficulties logged
   (or confirm "No new difficulties encountered").
